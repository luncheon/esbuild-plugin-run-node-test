{
  "name": "example",
  "version": "0.0.0",
  "main": "index.js",
  "scripts": {
    "serve": "node scripts/serve.mjs",
    "watch": "node scripts/watch.mjs",
    "test": "node scripts/test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.14.38",
    "esbuild-plugin-pipe": "^0.2.0",
    "esbuild-plugin-run-node-test": "file:.."
  },
  "dependencies": {
    "preact": "^10.7.1"
  }
}
